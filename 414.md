# IS 414 — Security

> Audit date: 2026-04-10. All line numbers verified against current source files.

---

## Score Summary

| Requirement | Points Possible | Estimated | Status |
|---|---|---|---|
| Confidentiality — HTTPS / TLS | 1.0 | **1.0** | ✅ |
| Confidentiality — HTTP → HTTPS redirect | 0.5 | **0.5** | ✅ |
| Auth — Username / password login | 3.0 | **3.0** | ✅ |
| Auth — NIST password policy (14+, no complexity) | 1.0 | **1.0** | ✅ |
| Auth — Endpoints protected appropriately | 1.0 | **1.0** | ✅ |
| Auth — RBAC: Admin-only CUD at the API level | 1.5 | **1.5** | ✅ |
| Integrity — Confirmation before delete | 1.0 | **1.0** | ✅ |
| Credentials — Secrets not in repo | 1.0 | **1.0** | ✅ |
| Privacy — GDPR privacy policy | 1.0 | **1.0** | ✅ |
| Privacy — Cookie consent (functional) | 1.0 | **1.0** | ✅ |
| Attack Mitigations — CSP header | 2.0 | **2.0** | ✅ |
| Availability — Publicly deployed | 4.0 | **4.0** | ✅ |
| **Additional — Google OAuth + TOTP MFA + Docker** | **2.0** | **2.0** | ✅ |
| **TOTAL** | **20.0** | **20.0** | ✅ |

> **Note on HTTP→HTTPS:** Railway handles TLS termination; the in-app `UseHttpsRedirection()` fires for any plaintext traffic that bypasses the proxy. Show both the code and call it out verbally.

---

## IS 414 VIDEO WALKTHROUGH — Step by Step

> Every item below must appear on screen in your recording. Graders only score what they see. Follow this order exactly. Target: under 10 minutes.

---

### STEP 1 — HTTPS / TLS Padlock *(~30 sec)*
**Points:** 1.0

1. Navigate to the **production site URL**
2. Point cursor at the browser address bar — show the padlock icon and `https://` clearly
3. Say: *"The site is served over HTTPS. Vercel provisions the TLS certificate for the frontend; Railway provisions it for the backend. All traffic is encrypted in transit."*

**Code to show:**
- [frontend/vercel.json](frontend/vercel.json) — rewrites all `/api/*` to Railway over HTTPS

---

### STEP 2 — HTTP → HTTPS Redirect *(~45 sec)*
**Points:** 0.5

1. Say: *"Railway enforces HTTPS at its edge proxy. In the application code, ForwardedHeaders runs first so ASP.NET reads the real https:// scheme, then UseHttpsRedirection enforces it at the app layer."*
2. Show the following three code locations:

**Code to show:**
- [backend/HopeHaven.API/Program.cs:13-19](backend/HopeHaven.API/Program.cs#L13-L19) — `ForwardedHeadersOptions`: clears `KnownNetworks` and `KnownProxies` so Railway's proxy is always trusted
  ```csharp
  options.ForwardedHeaders = ForwardedHeaders.XForwardedFor | ForwardedHeaders.XForwardedProto | ForwardedHeaders.XForwardedHost;
  options.KnownNetworks.Clear();
  options.KnownProxies.Clear();
  ```
- [backend/HopeHaven.API/Program.cs:181](backend/HopeHaven.API/Program.cs#L181) — `app.UseForwardedHeaders()` — must be **first** in the pipeline
- [backend/HopeHaven.API/Program.cs:222](backend/HopeHaven.API/Program.cs#L222) — `app.UseHttpsRedirection()` — unconditional, not inside `if (IsDevelopment())`

---

### STEP 3 — Username / Password Login *(~60 sec)*
**Points:** 3.0

**Part A — Failed login:**
1. Navigate to `/login`
2. Enter a valid email, wrong password → show the red error banner that appears
3. Say: *"Invalid credentials are rejected with a clear error message."*

**Part B — Successful login:**
1. Enter correct admin credentials (email + 14+ char password)
2. Click Sign In → show the redirect to `/admin` dashboard
3. Say: *"Successful login redirects to the admin dashboard."*

**Code to show:**
- [frontend/src/pages/public/LoginPage.tsx:44-62](frontend/src/pages/public/LoginPage.tsx#L44-L62) — `handleSubmit()`: calls `loginUser()`, catches errors, sets `serverError` state
- [frontend/src/pages/public/LoginPage.tsx:33-42](frontend/src/pages/public/LoginPage.tsx#L33-L42) — role-based redirect after login (Admin → `/admin`, Donor → `/donor`)
- [backend/HopeHaven.API/Program.cs:34-36](backend/HopeHaven.API/Program.cs#L34-L36) — `AddIdentityApiEndpoints<ApplicationUser>()` — registers Identity with roles and EF stores
- [backend/HopeHaven.API/Program.cs:229](backend/HopeHaven.API/Program.cs#L229) — `MapGroup("/api/auth").MapIdentityApi<ApplicationUser>()` — exposes `/api/auth/login`, `/api/auth/register`, etc.
- [backend/HopeHaven.API/Data/AuthIdentityDbContext.cs](backend/HopeHaven.API/Data/AuthIdentityDbContext.cs) — Identity stored in a separate Supabase PostgreSQL database

---

### STEP 4 — NIST Password Policy (14+ chars, no complexity) *(~60 sec)*
**Points:** 1.0

**Part A — Short password rejected:**
1. Navigate to `/register`
2. Enter a password shorter than 14 characters (e.g., `short123`)
3. Show the error message: *"Password must be at least 14 characters."* (client-side, before hitting the API)

**Part B — Long simple password accepted:**
1. Enter a password of 14+ chars with no uppercase/digits/symbols (e.g., `mylongpassword`)
2. Show registration succeeds
3. Say: *"We follow NIST SP 800-63B — length over complexity. 14 characters minimum, no digit, uppercase, or symbol requirements."*

**Code to show:**
- [backend/HopeHaven.API/Program.cs:87-95](backend/HopeHaven.API/Program.cs#L87-L95) — the full password policy block:
  ```csharp
  options.Password.RequireDigit           = false;
  options.Password.RequireLowercase       = false;
  options.Password.RequireNonAlphanumeric = false;
  options.Password.RequireUppercase       = false;
  options.Password.RequiredLength         = 14;
  options.Password.RequiredUniqueChars    = 1;
  ```
- [frontend/src/pages/public/RegisterPage.tsx:34-37](frontend/src/pages/public/RegisterPage.tsx#L34-L37) — client-side guard: `if (password.length < 14)` blocks submission before the API call

---

### STEP 5 — Public Pages Accessible Without Login *(~30 sec)*
**Points:** part of Auth 1.0

1. Make sure you are **logged out** (or use an incognito window)
2. Navigate to each of these — show they all load without a login prompt:
   - `/` — Landing page
   - `/impact` — Impact dashboard
   - `/privacy` — Privacy policy
   - `/donor-wall` — Donor wall
3. Say: *"Public pages are fully accessible to unauthenticated visitors."*

**Code to show:**
- [frontend/src/App.tsx](frontend/src/App.tsx) — public routes defined **outside** any `<ProtectedRoute>` wrapper
- [backend/HopeHaven.API/Controllers/DashboardController.cs](backend/HopeHaven.API/Controllers/DashboardController.cs) — `[AllowAnonymous]` on the public GET endpoints
- [backend/HopeHaven.API/Controllers/PublicImpactSnapshotsController.cs](backend/HopeHaven.API/Controllers/PublicImpactSnapshotsController.cs) — `[AllowAnonymous]` on GET

---

### STEP 6 — Protected Endpoints Return 401 When Unauthenticated *(~45 sec)*
**Points:** part of Auth 1.0

1. While **logged out**, try navigating directly to `/admin` — show the browser redirects to `/login`
2. Open **DevTools → Network**, try fetching `/api/residents` — show `401 Unauthorized` in the response
3. Say: *"Unauthenticated requests to protected API endpoints return 401. The frontend ProtectedRoute redirects to /login."*

**Code to show:**
- [frontend/src/App.tsx](frontend/src/App.tsx) — `ProtectedRoute` component: checks `isAuthenticated`, redirects to `/login` if false
- [backend/HopeHaven.API/Program.cs:225-226](backend/HopeHaven.API/Program.cs#L225-L226) — `app.UseAuthentication()` then `app.UseAuthorization()`
- [backend/HopeHaven.API/Program.cs:75-84](backend/HopeHaven.API/Program.cs#L75-L84) — `DefaultPolicy` requires `IdentityConstants.ApplicationScheme`; cookie returns 401/403 instead of redirect (lines 106-115)

---

### STEP 7 — RBAC: Admin-Only CUD (Donor Gets 403) *(~60 sec)*
**Points:** 1.5

1. Log in as the **Donor** account (no MFA)
2. Open **DevTools → Console**
3. Run this fetch to hit an Admin-only DELETE:
   ```js
   fetch('/api/residents/1', { method: 'DELETE' }).then(r => console.log(r.status))
   ```
4. Show `403 Forbidden` in the console
5. Log in as **Admin**, navigate to a management page (e.g., Donor Management), and successfully delete a record via the UI
6. Say: *"RBAC is enforced at the API level. The ManageContent policy requires the Admin role — Donors receive 403 on any CUD operation."*

**Code to show:**
- [backend/HopeHaven.API/Controllers/Base/CrudController.cs](backend/HopeHaven.API/Controllers/Base/CrudController.cs) — `[Authorize(Policy = AuthPolicies.ManageContent)]` on Create (POST), Update (PUT), Delete (DELETE)
- [backend/HopeHaven.API/Data/AuthPolicies.cs](backend/HopeHaven.API/Data/AuthPolicies.cs) — `ManageContent = "ManagingContent"`
- [backend/HopeHaven.API/Program.cs:80-83](backend/HopeHaven.API/Program.cs#L80-L83) — `ManageContent` policy: `RequireRole(AuthRoles.Admin)`; `DonorAccess` policy: Donor or Admin
- [backend/HopeHaven.API/Controllers/MyDonationsController.cs](backend/HopeHaven.API/Controllers/MyDonationsController.cs) — `[Authorize]` (any authenticated user) — donors can only read their own history

---

### STEP 8 — Delete Confirmation Dialog *(~30 sec)*
**Points:** 1.0

1. Log in as **Admin**, navigate to `/admin/donors` (Donor Management)
2. Click the **Delete** button on any donor record
3. Show the confirmation modal — it must ask you to confirm and warn *"This action cannot be undone."*
4. Click **Cancel** first (to prove it does not delete), then click **Delete** to confirm
5. Say: *"Every destructive action requires explicit confirmation via an alertdialog modal before executing."*

**Code to show:**
- [frontend/src/components/shared/DeleteConfirmDialog.tsx](frontend/src/components/shared/DeleteConfirmDialog.tsx) — reusable modal with `role="alertdialog"`, Cancel + Delete buttons, loading state
- [frontend/src/pages/admin/DonorManagement.tsx](frontend/src/pages/admin/DonorManagement.tsx) — `<DeleteConfirmDialog>` wired to the delete button
- Briefly mention it is also used in [frontend/src/pages/admin/HomeVisitations.tsx](frontend/src/pages/admin/HomeVisitations.tsx), [frontend/src/pages/admin/ProcessRecordings.tsx](frontend/src/pages/admin/ProcessRecordings.tsx), and [frontend/src/pages/admin/CaseloadInventory.tsx](frontend/src/pages/admin/CaseloadInventory.tsx)

---

### STEP 9 — Credentials: Secrets Not in the Repo *(~60 sec)*
**Points:** 1.0

1. Open [backend/HopeHaven.API/appsettings.json](backend/HopeHaven.API/appsettings.json) — point out that the connection string uses `localhost` and the admin password is `"ChangeThisPassword14!"` — clearly **dev placeholders only**
2. Open [.gitignore](.gitignore) — show that `appsettings.Development.json`, `appsettings.*.json`, `.env`, `.env.local`, `.env.*.local` are all excluded from the repo
3. Open **Railway → your backend service → Variables tab** — show the real credential keys exist: `ConnectionStrings__Default`, `ConnectionStrings__IdentityConnection`, `Authentication__Google__ClientId`, etc. (you don't need to reveal values)
4. Say: *"Real credentials live only in Railway environment variables. The only appsettings file committed to GitHub is the one with localhost placeholders."*

**Code to show:**
- [backend/HopeHaven.API/appsettings.json:1-21](backend/HopeHaven.API/appsettings.json#L1-L21) — all localhost/placeholder values, nothing real
- [.gitignore](.gitignore) — lines excluding `appsettings.*.json`, `.env`, `.env.local`
- [backend/HopeHaven.API/Program.cs:44-59](backend/HopeHaven.API/Program.cs#L44-L59) — Google credentials read from `builder.Configuration` (Railway env vars at runtime); conditional activation if not configured

---

### STEP 10 — Privacy Policy (GDPR) *(~45 sec)*
**Points:** 1.0

1. Scroll to the **footer** of the landing page — show the **Privacy Policy** link
2. Click it — navigate to `/privacy`
3. Slowly scroll through all 10 sections — keep each on screen long enough to read the heading
4. Say: *"The privacy policy is tailored to Hope Haven. It covers all GDPR-required sections: Who We Are, Data Collected, How It's Used, Cookies, Data Sharing, Retention, Your Rights, Security, Changes, and Contact."*

**Code to show:**
- [frontend/src/pages/public/PrivacyPage.tsx:27-130](frontend/src/pages/public/PrivacyPage.tsx#L27-L130) — all 10 GDPR-aligned `<Section>` components
- [frontend/src/components/shared/PublicLayout.tsx:131](frontend/src/components/shared/PublicLayout.tsx#L131) — `<Link to="/privacy">` in the footer

---

### STEP 11 — Cookie Consent (Functional: Accept + Decline + Reset) *(~90 sec)*
**Points:** 1.0

1. Open an **incognito/private window** so localStorage is empty
2. Navigate to the site — the cookie consent banner **must appear** at the bottom
3. Show both **Accept** and **Decline** buttons
4. Click **Decline** — banner disappears
5. Refresh — banner does **not** reappear (preference persisted)
6. Navigate to `/privacy` — **scroll down to Section 4 (Cookies)** — show the **"Reset Cookie Preferences"** button
7. Click it — page reloads, banner comes back
8. Click **Accept** — banner disappears
9. Say: *"Cookie consent is fully functional. Both choices persist to localStorage under the key 'hh_cookie_consent'. The privacy page allows users to reset their preference at any time."*

**Code to show:**
- [frontend/src/components/shared/CookieConsentBanner.tsx:23-35](frontend/src/components/shared/CookieConsentBanner.tsx#L23-L35) — `<div>` containing both Decline (line 24) and Accept (line 30) buttons
- [frontend/src/contexts/CookieConsentContext.tsx:22-29](frontend/src/contexts/CookieConsentContext.tsx#L22-L29) — `acknowledgeConsent()` stores `'accepted'`; `declineConsent()` stores `'declined'`
- [frontend/src/contexts/CookieConsentContext.tsx:13-16](frontend/src/contexts/CookieConsentContext.tsx#L13-L16) — `readInitial()` reads localStorage; banner stays hidden for either choice
- [frontend/src/pages/public/PrivacyPage.tsx:17-20](frontend/src/pages/public/PrivacyPage.tsx#L17-L20) — `clearConsent()` removes the key and reloads (button is at lines 66-71 in Section 4)

---

### STEP 12 — CSP Header in DevTools *(~60 sec)*
**Points:** 2.0

1. On the **live production site**, open **DevTools → Network** tab
2. Click on any document or API request (e.g., the main page load or `/api/dashboard/metrics`)
3. Open the **Response Headers** section — find `content-security-policy`
4. Read out the key directives on screen:
   - `default-src 'self'` — only same-origin resources
   - `script-src 'self'` — no external scripts
   - `img-src 'self' data:` — allows data URIs (QR codes for MFA)
   - `frame-ancestors 'none'` — prevents clickjacking
   - `object-src 'none'` — blocks Flash/plugin injection
5. Open **DevTools → Console** — show **zero red CSP violation errors**
6. Say: *"The CSP is set as an HTTP response header via server-side middleware — not a meta tag. It is visible in DevTools and restricts every resource type."*

**Code to show:**
- [backend/HopeHaven.API/Infrastructure/SecurityHeaders.cs:5-14](backend/HopeHaven.API/Infrastructure/SecurityHeaders.cs#L5-L14) — the full CSP string constant
- [backend/HopeHaven.API/Program.cs:220](backend/HopeHaven.API/Program.cs#L220) — `app.UseSecurityHeaders()` registered in the pipeline (before CORS and auth)

---

### STEP 13 — Site Publicly Accessible *(~30 sec)*
**Points:** 4.0

1. With **no VPN connected**, navigate to the production site URL
2. Show the site loads completely — land on the home page
3. Say: *"The site is publicly deployed. Frontend on Vercel, backend on Railway, containerized with Docker. All API calls route through the Vercel proxy — no VPN or credentials needed to access public pages."*

**Code to show:**
- [frontend/vercel.json](frontend/vercel.json) — Vercel rewrites `/api/*` and `/signin-google` to Railway
- [backend/HopeHaven.API/Dockerfile](backend/HopeHaven.API/Dockerfile) — multi-stage Docker build; deployed as a container on Railway

---

### STEP 14 — Google OAuth ("Continue with Google") *(~60 sec)*
**Points:** part of Additional 2.0

1. Navigate to `/login` — show the **"Continue with Google"** button below the sign-in form
2. Navigate to `/register` — show the **same "Continue with Google"** button on registration
3. Click it from `/login` — Google's account picker appears
4. Complete the flow — show that you are signed in to Hope Haven after returning from Google
5. Say: *"Google OAuth is on both login and registration. New Google users get a Donor account created automatically. The account picker is always forced."*

**Code to show:**
- [backend/HopeHaven.API/Program.cs:43-59](backend/HopeHaven.API/Program.cs#L43-L59) — `AddGoogle()` registration: reads credentials from configuration, sets SameSite=None on correlation cookie
- [backend/HopeHaven.API/Controllers/AuthController.cs:62-78](backend/HopeHaven.API/Controllers/AuthController.cs#L62-L78) — `ExternalLogin()`: initiates OAuth challenge; line 74 forces `"select_account"` via `props.Items["prompt"]`
- [backend/HopeHaven.API/Controllers/AuthController.cs:81-117](backend/HopeHaven.API/Controllers/AuthController.cs#L81-L117) — `ExternalLoginCallback()`: finds or creates local account, assigns Donor role
- [frontend/src/pages/public/LoginPage.tsx:199-225](frontend/src/pages/public/LoginPage.tsx#L199-L225) — Google button rendered when provider list is non-empty

---

### STEP 15 — MFA / TOTP (Authenticator App + Recovery Codes) *(~90 sec)*
**Points:** part of Additional 2.0

**Part A — Show MFA setup:**
1. Log in with the account that has MFA enabled
2. Navigate to the MFA management page (`/admin/mfa` or similar)
3. Show the **QR code** — explain it's scanned into an authenticator app
4. Show the **recovery codes** below the QR code
5. Say: *"Users can enable TOTP-based MFA. The QR code encodes an otpauth://totp/ URI with the user's email and a shared secret. Recovery codes are provided for fallback access."*

**Part B — Show MFA prompt at login:**
1. Log out; log back in with the MFA-enabled account credentials
2. Click **"Have an authenticator or recovery code?"**
3. Show the 6-digit code field and recovery code field appear
4. Enter the TOTP code from the authenticator app → show successful login
5. Say: *"When MFA is enabled, login prompts for the TOTP code from the authenticator app."*

**Code to show:**
- [frontend/src/pages/admin/ManageMFAPage.tsx:23-29](frontend/src/pages/admin/ManageMFAPage.tsx#L23-L29) — builds `otpauth://totp/` URI with issuer `"Hope Haven"` and the user's email
- [frontend/src/pages/admin/ManageMFAPage.tsx:42](frontend/src/pages/admin/ManageMFAPage.tsx#L42) — `QRCode.toDataURL(uri, { width: 224, margin: 1 })` generates the QR code image
- [frontend/src/pages/admin/ManageMFAPage.tsx:45-55](frontend/src/pages/admin/ManageMFAPage.tsx#L45-L55) — `handleEnable()` submits the 6-digit code to activate MFA
- [frontend/src/pages/public/LoginPage.tsx:123-167](frontend/src/pages/public/LoginPage.tsx#L123-L167) — MFA section: authenticator code input + recovery code input; toggled by the "Have an authenticator?" button

---

### Pre-Recording Checklist

Before you hit record, verify all of these:
- [ ] Production site loads at the public URL with `https://` and padlock
- [ ] DevTools → Network → any request response → `content-security-policy` header is present
- [ ] DevTools → Console → **zero** red CSP violation errors
- [ ] Google "Continue with Google" button shows on `/login` and `/register`
- [ ] Admin account (no MFA) credentials ready
- [ ] Donor account (no MFA, has donation history) credentials ready
- [ ] Account with MFA enabled + authenticator app ready
- [ ] Incognito window ready for cookie consent demo
- [ ] Railway **Variables** tab ready to show (key names only, not values)

---

## Full Evidence Checklist

### Confidentiality

- [x] **HTTPS / TLS on all public connections**
  - Vercel (frontend) and Railway (backend) both provision TLS automatically
  - All traffic uses `https://`

- [x] **Redirect HTTP → HTTPS**
  - [Program.cs:13-19](backend/HopeHaven.API/Program.cs#L13-L19) — `ForwardedHeadersOptions` trusts Railway proxy
  - [Program.cs:181](backend/HopeHaven.API/Program.cs#L181) — `app.UseForwardedHeaders()` first in pipeline
  - [Program.cs:222](backend/HopeHaven.API/Program.cs#L222) — `app.UseHttpsRedirection()` unconditional

---

### Authentication

- [x] **Username / password authentication (ASP.NET Identity)**

  | File | Lines | What it does |
  |---|---|---|
  | `Program.cs` | 34–36 | `AddIdentityApiEndpoints<ApplicationUser>()` + roles + EF stores |
  | `Program.cs` | 229 | `MapIdentityApi<ApplicationUser>()` — exposes identity endpoints |
  | `LoginPage.tsx` | 44–62 | Login form submit handler |
  | `RegisterPage.tsx` | 26–48 | Registration submit handler |
  | `AuthIdentityDbContext.cs` | — | Separate Identity DB context (Supabase) |

- [x] **Visitors can browse public pages without login**
  - Public routes in `App.tsx`: `/`, `/impact`, `/login`, `/register`, `/logout`, `/privacy`, `/referral`, `/donate`, `/donor-wall`, `/social` — no `ProtectedRoute`
  - `PublicImpactSnapshotsController.cs` — `[AllowAnonymous]` on GET
  - `DashboardController.cs` — `[AllowAnonymous]` on public GET endpoints

- [x] **Authenticated users can view IS413 pages**
  - `ProtectedRoute` in `App.tsx` gates admin and donor routes
  - Admin routes require `requiredRoles={['Admin']}`

- [x] **NIST password policy (14+ chars, no complexity)**

  [Program.cs:87-95](backend/HopeHaven.API/Program.cs#L87-L95):
  ```csharp
  options.Password.RequireDigit           = false;
  options.Password.RequireLowercase       = false;
  options.Password.RequireNonAlphanumeric = false;
  options.Password.RequireUppercase       = false;
  options.Password.RequiredLength         = 14;
  options.Password.RequiredUniqueChars    = 1;
  ```
  Client-side guard: [RegisterPage.tsx:34-37](frontend/src/pages/public/RegisterPage.tsx#L34-L37)

- [x] **All API endpoints protected appropriately**

  Open (correctly public): `AuthController` auth/me, providers, external-login, external-callback; `ReferralsController`; `PublicImpactSnapshotsController` GET; `DashboardController` public GETs

  Protected controllers (all require Auth):

  | Controller | Auth |
  |---|---|
  | `ResidentsController` | `[Authorize(Roles="Admin")]` |
  | `DonationsController` | `[Authorize(Roles="Admin")]` |
  | `SupportersController` | `[Authorize(Roles="Admin")]` |
  | `EducationRecordsController` | `[Authorize(Roles="Admin")]` + CrudController base |
  | `HealthWellbeingRecordsController` | `[Authorize(Roles="Admin")]` + CrudController base |
  | `DashboardController` | `[Authorize(Roles="Admin")]` (2 GETs are `[AllowAnonymous]`) |
  | `ReportsController` | `[Authorize(Roles="Admin")]` |
  | `SocialMediaPostsController` | `[Authorize(Roles="Admin")]` |
  | `PredictController` | `[Authorize(Roles="Admin")]` |
  | `HomeVisitationsController` | `[Authorize(Roles="Admin")]` |
  | `IncidentReportsController` | `[Authorize(Roles="Admin")]` |
  | `InterventionPlansController` | `[Authorize(Roles="Admin")]` |
  | `ProcessRecordingsController` | `[Authorize(Roles="Admin")]` |
  | `SafehousesController` | `[Authorize(Roles="Admin")]` |
  | `PartnersController` | `[Authorize(Roles="Admin")]` |
  | `DonationAllocationsController` | `[Authorize(Roles="Admin")]` |
  | `InKindDonationItemsController` | `[Authorize(Roles="Admin")]` |
  | `UsersController` | `[Authorize(Roles="Admin")]` |
  | `MyDonationsController` | `[Authorize]` (any logged-in user) |
  | `CrudController` base | `[Authorize]` + `[Authorize(Policy="ManageContent")]` on POST/PUT/DELETE |

---

### RBAC

- [x] **Only Admin can CUD; Donors see their history; public sees limited pages**

  Every POST, PUT, DELETE in `CrudController.cs` has `[Authorize(Policy = AuthPolicies.ManageContent)]`.

  `ManageContent` policy → [Program.cs:80-81](backend/HopeHaven.API/Program.cs#L80-L81) → `RequireRole(AuthRoles.Admin)`

  `DonorAccess` policy → [Program.cs:82-83](backend/HopeHaven.API/Program.cs#L82-L83) → `RequireRole(AuthRoles.Donor, AuthRoles.Admin)` — used by `MyDonationsController`

---

### Integrity

- [x] **Delete requires confirmation**

  `DeleteConfirmDialog` ([frontend/src/components/shared/DeleteConfirmDialog.tsx](frontend/src/components/shared/DeleteConfirmDialog.tsx)) used in:
  - `DonorManagement.tsx`
  - `HomeVisitations.tsx`
  - `ProcessRecordings.tsx`
  - `CaseloadInventory.tsx`
  - `UserManagement.tsx`

---

### Credentials

- [x] **Secrets stored securely, not in repo**

  | Secret | Where |
  |---|---|
  | Supabase operational DB connection string | Railway env var `ConnectionStrings__Default` |
  | Supabase identity DB connection string | Railway env var `ConnectionStrings__IdentityConnection` |
  | Google OAuth Client ID + Secret | Railway env vars `Authentication__Google__ClientId/Secret` |
  | Admin seed email + password | Railway env vars `GenerateDefaultIdentityAdmin__Email/Password` |

  `appsettings.json` (the only committed settings file) contains only `localhost` placeholders.
  `.gitignore`: excludes `appsettings.Development.json`, `appsettings.*.json`, `.env`, `.env.local`, `.env.*.local`

---

### Privacy

- [x] **GDPR privacy policy, tailored to site, linked from footer**
  - Page: [frontend/src/pages/public/PrivacyPage.tsx:27-130](frontend/src/pages/public/PrivacyPage.tsx#L27-L130) — 10 sections (Who We Are, Data Collected, How We Use, Cookies, Data Sharing, Retention, Rights, Security, Changes, Contact)
  - Footer link: [frontend/src/components/shared/PublicLayout.tsx:131](frontend/src/components/shared/PublicLayout.tsx#L131)

- [x] **GDPR cookie consent notification (functional)**
  - Banner: [frontend/src/components/shared/CookieConsentBanner.tsx](frontend/src/components/shared/CookieConsentBanner.tsx) — Accept + Decline buttons (lines 23-35)
  - Context: [frontend/src/contexts/CookieConsentContext.tsx](frontend/src/contexts/CookieConsentContext.tsx) — localStorage key `hh_cookie_consent`; `acknowledgeConsent()` stores `'accepted'` (line 23); `declineConsent()` stores `'declined'` (line 27); `readInitial()` hides banner for either value (lines 13-16)
  - Reset: [frontend/src/pages/public/PrivacyPage.tsx:66-71](frontend/src/pages/public/PrivacyPage.tsx#L66-L71) — "Reset Cookie Preferences" button in Section 4 (Cookies)

---

### Attack Mitigations

- [x] **CSP header set via middleware (not meta tag)**
  - Middleware: [backend/HopeHaven.API/Infrastructure/SecurityHeaders.cs:5-14](backend/HopeHaven.API/Infrastructure/SecurityHeaders.cs#L5-L14)
  - Registered: [Program.cs:220](backend/HopeHaven.API/Program.cs#L220) — `app.UseSecurityHeaders()`
  - Policy:
    ```
    default-src 'self';
    script-src 'self';
    style-src 'self' 'unsafe-inline';
    img-src 'self' data:;
    connect-src 'self';
    font-src 'self';
    base-uri 'self';
    frame-ancestors 'none';
    object-src 'none'
    ```
  - `style-src 'unsafe-inline'` — required for Recharts inline styles
  - `img-src data:` — required for QR code data URIs on MFA setup page

---

### Availability

- [x] **Site publicly accessible**
  - Frontend: Vercel (public HTTPS URL)
  - Backend: Railway (Docker container, proxied through Vercel via `vercel.json` rewrites)

---

### Additional Security Features ✅ (2/2 pts)

#### 1. Google OAuth (Third-Party Authentication)
- "Continue with Google" button on both `/login` and `/register`
- New Google users automatically get a Donor account and role
- Account picker always forced via `props.Items["prompt"] = "select_account"` ([AuthController.cs:74](backend/HopeHaven.API/Controllers/AuthController.cs#L74))

| File | Lines | What it does |
|---|---|---|
| `Program.cs` | 43–59 | Registers Google OAuth (conditional on secrets) |
| `AuthController.cs` | 62–78 | `ExternalLogin()` — initiates OAuth challenge |
| `AuthController.cs` | 81–117 | `ExternalLoginCallback()` — finds/creates user, assigns Donor role |
| `LoginPage.tsx` | 199–225 | Google button rendered when providers list is non-empty |
| `RegisterPage.tsx` | 138–164 | Same Google button on registration page |

#### 2. TOTP / Authenticator App MFA
- QR code generation for setup (`otpauth://totp/` URI at lines 23-29)
- Enable / disable MFA, recovery codes displayed and resettable
- TOTP + recovery code entry fields on login page

| File | Lines | What it does |
|---|---|---|
| `ManageMFAPage.tsx` | 23–29 | Builds `otpauth://totp/` URI with issuer and email |
| `ManageMFAPage.tsx` | 42 | `QRCode.toDataURL(uri, ...)` generates QR image |
| `ManageMFAPage.tsx` | 45–55 | `handleEnable()` submits 6-digit code to activate MFA |
| `LoginPage.tsx` | 123–167 | MFA code + recovery code fields; toggle shown when MFA is needed |

#### 3. Docker Container Deployment
- Backend deployed as a Docker container on Railway (not a plain VM)
- [backend/HopeHaven.API/Dockerfile](backend/HopeHaven.API/Dockerfile) — multi-stage build: SDK image for compile, ASP.NET runtime image for production (minimal attack surface)

> **Note for graders:** Use admin/donor accounts WITHOUT MFA for basic testing. A separate MFA-enabled account is available for the MFA demo only.
