# IS 414 — Security

> Audit date: 2026-04-08 (re-audited after pull). All items verified against current `main` branch.
> **Replaces:** `securitychecklist.md` (you can delete that file)

---

## IS 414 VIDEO WALKTHROUGH — Step by Step

> Every item below must appear on screen in your recording. Graders only score what they see. Follow this order exactly.

---

### STEP 1 — HTTPS / TLS Padlock
**Points:** 1.0

**What to do on screen:**
1. Navigate to your production Railway URL (e.g., `https://intex2-production.up.railway.app`)
2. Point camera / cursor at the browser address bar — the padlock icon and `https://` must both be clearly visible
3. Say: *"The site is served over HTTPS. Railway provisions a TLS certificate automatically."*

**Code to show:**
- `backend/HopeHaven.API/Dockerfile` — shows this is deployed as a production container on Railway
- `backend/HopeHaven.API/railway.toml` — shows Railway is the deployment platform

---

### STEP 2 — HTTP → HTTPS Redirect
**Points:** 0.5

**What to do on screen:**
1. Open DevTools → Console or just explain verbally while pointing at the address bar
2. Say: *"Railway enforces an HTTP-to-HTTPS redirect at the proxy level. Our code also calls `UseHttpsRedirection()` unconditionally so the redirect is enforced at the application layer too."*
3. Pull up the code and highlight:

**Code to show:**
- `backend/HopeHaven.API/Program.cs` **line 144** — `app.UseForwardedHeaders()` — must run first so ASP.NET can see the real `https://` scheme from Railway's proxy
- `backend/HopeHaven.API/Program.cs` **line 167** — `app.UseHttpsRedirection()` — unconditional, applies in all environments (not inside an `if (IsDevelopment())` block)
- Say: *"ForwardedHeaders runs first so the HTTPS scheme from Railway's reverse proxy is visible. UseHttpsRedirection then enforces the redirect at the application level."*

---

### STEP 3 — Username / Password Login
**Points:** 3.0

**What to do on screen:**

**Part A — Show a failed login:**
1. Navigate to `/login`
2. Enter a wrong password for a valid email
3. Show the error message that appears (red error text on screen)
4. Say: *"Invalid credentials are rejected and an error message is displayed."*

**Part B — Show a successful login:**
1. Enter the correct admin credentials (email + 14+ char password)
2. Click Sign In
3. Show redirect to `/admin` dashboard
4. Say: *"Successful login redirects to the admin dashboard."*

**Code to show:**
- `frontend/src/pages/public/LoginPage.tsx` **lines 41–58** — `handleSubmit()`, calls `loginUser()`, catches errors, sets `serverError` state
- `frontend/src/pages/public/LoginPage.tsx` **lines 31–39** — redirect logic after login (role-based: Admin → `/admin`, Donor → `/donor`)
- `frontend/src/utils/authAPI.ts` **lines 59–74** — `loginUser()` API call hitting ASP.NET Identity
- `backend/HopeHaven.API/Program.cs` **line 32** — `AddIdentityApiEndpoints<ApplicationUser>()` — registers the Identity system
- `backend/HopeHaven.API/Program.cs` **line 174** — `MapIdentityApi<ApplicationUser>()` — exposes `/api/auth/login`, `/api/auth/register`, etc.
- `backend/HopeHaven.API/Data/AuthIdentityDbContext.cs` — Identity DB context (stored in separate Supabase schema)

---

### STEP 4 — NIST Password Policy (14+ characters, no complexity)
**Points:** 1.0

**What to do on screen:**

**Part A — Show a short password failing:**
1. Navigate to `/register`
2. Enter a password shorter than 14 characters (e.g., `short123`)
3. Show the error message: *"Password must be at least 14 characters"*

**Part B — Show a long password succeeding:**
1. Enter a password of 14+ characters with no special complexity (e.g., `mylongpassword`)
2. Show the registration succeed (or validation pass)
3. Say: *"We follow NIST SP 800-63B — length is required, complexity is not. 14 characters minimum, no digit/uppercase/symbol rules."*

**Code to show:**
- `backend/HopeHaven.API/Program.cs` **lines 67–76**:
  ```csharp
  options.Password.RequireDigit           = false;
  options.Password.RequireLowercase       = false;
  options.Password.RequireNonAlphanumeric = false;
  options.Password.RequireUppercase       = false;
  options.Password.RequiredLength         = 14;
  options.Password.RequiredUniqueChars    = 1;
  ```
- `frontend/src/pages/public/RegisterPage.tsx` **lines 22–24** — client-side check enforcing the same 14-char minimum before the API call

---

### STEP 5 — Public Pages Accessible Without Login
**Points:** part of Auth 1.0

**What to do on screen:**
1. Make sure you are logged out (or open an incognito window)
2. Navigate to each of these pages — show they load without a login prompt:
   - `/` — Landing page
   - `/impact` — Impact dashboard
   - `/login` — Login page
   - `/privacy` — Privacy policy
3. Say: *"Public pages are accessible to anyone with no authentication required."*

**Code to show:**
- `frontend/src/App.tsx` **lines 69–77** — public routes defined outside `<ProtectedRoute>` wrapper
- `backend/HopeHaven.API/Controllers/DashboardController.cs` **line 13** — `[AllowAnonymous]` on `GetMetrics()` (feeds landing page stats)
- `backend/HopeHaven.API/Controllers/DashboardController.cs` **line 86** — `[AllowAnonymous]` on `GetSafehouseSummary()`
- `backend/HopeHaven.API/Controllers/PublicImpactSnapshotsController.cs` **line 17** — `[AllowAnonymous]` on GET

---

### STEP 6 — Protected Pages Redirect to Login When Unauthenticated
**Points:** part of Auth 1.0

**What to do on screen:**
1. While still logged out, try navigating directly to `/admin` or `/admin/residents`
2. Show the browser redirects to `/login`
3. Open DevTools → Network tab, hit `/api/residents` directly — show `401 Unauthorized` response
4. Say: *"Unauthenticated requests to protected API endpoints return 401. The frontend also redirects to login."*

**Code to show:**
- `frontend/src/App.tsx` **lines 49–57** — `ProtectedRoute` component: checks `isAuthenticated`, redirects to `/login` if false
- `frontend/src/App.tsx` **line 126** — admin routes wrapped in `<ProtectedRoute requiredRoles={['Admin']}>` 
- `backend/HopeHaven.API/Controllers/ResidentsController.cs` **line 10** — `[Authorize(Roles = "Admin")]` at class level
- `backend/HopeHaven.API/Program.cs` **lines 170–171** — `UseAuthentication()` and `UseAuthorization()` in the pipeline

---

### STEP 7 — RBAC: Admin-Only CUD (Donor Gets 403)
**Points:** 1.5

**What to do on screen:**
1. Log in as the **Donor** account (no MFA)
2. Open DevTools → Network tab
3. Try to perform a write action that only Admins can do — e.g., manually send a `POST` or `DELETE` to `/api/residents` via DevTools Console:
   ```js
   fetch('/api/residents', { method: 'POST', headers: {'Content-Type':'application/json'}, body: '{}' })
     .then(r => console.log(r.status))
   ```
4. Show the `403 Forbidden` response in the console
5. Log in as **Admin** and show the same action succeeds
6. Say: *"RBAC is enforced at the API level. Donors get 403 on any create, update, or delete. The ManageContent policy requires the Admin role."*

**Code to show:**
- `backend/HopeHaven.API/Controllers/Base/CrudController.cs` **line 31** — `[Authorize(Policy = AuthPolicies.ManageContent)]` on `Create()`
- `backend/HopeHaven.API/Controllers/Base/CrudController.cs` **line 40** — same on `Update()`
- `backend/HopeHaven.API/Controllers/Base/CrudController.cs` **line 58** — same on `Delete()`
- `backend/HopeHaven.API/Data/AuthPolicies.cs` **line 5** — `ManageContent = "ManagingContent"` constant
- `backend/HopeHaven.API/Program.cs` **lines 61–62** — policy defined: `RequireRole(AuthRoles.Admin)` — only Admin role satisfies it
- `backend/HopeHaven.API/Controllers/ResidentsController.cs` **line 10** — `[Authorize(Roles = "Admin")]` — entire controller Admin-only
- `backend/HopeHaven.API/Controllers/MyDonationsController.cs` **line 12** — `[Authorize]` (no role) — any logged-in user can read their own donations

---

### STEP 8 — Delete Confirmation Dialog
**Points:** 1.0

**What to do on screen:**
1. Log in as Admin, navigate to `/admin/donors` (Donor Management)
2. Click the Delete button on any donor record
3. Show the confirmation dialog that appears — it must ask you to confirm before deleting
4. Click Cancel first (to show it does not delete), then click Delete to confirm
5. Say: *"Every destructive action requires an explicit confirmation before it executes."*

**Code to show:**
- `frontend/src/components/shared/DeleteConfirmDialog.tsx` **lines 1–70** — the reusable modal component, uses `role="alertdialog"` for accessibility
- `frontend/src/pages/admin/DonorManagement.tsx` **lines 594–598** — where `<DeleteConfirmDialog>` is rendered
- `frontend/src/pages/admin/HomeVisitations.tsx` **lines 516–528** — same component used
- `frontend/src/pages/admin/ProcessRecordings.tsx` **lines 558–570** — same component used

---

### STEP 9 — Credentials: Secrets Not in the Repo
**Points:** 1.0

**What to do on screen:**
1. Open `backend/HopeHaven.API/appsettings.json` in the editor — point out that the connection string is `localhost/postgres/postgres` (a dev placeholder, not a real password)
2. Open `.gitignore` at the repo root — show `.env`, `.env.local`, `appsettings.Development.json` are all ignored
3. Briefly open Railway → your backend service → **Variables** tab — show the real credentials are stored there as environment variables (you don't need to reveal the values, just show the keys exist)
4. Say: *"Real credentials live in Railway environment variables and .NET user secrets locally. appsettings.json only contains localhost placeholders."*

**Code to show:**
- `backend/HopeHaven.API/appsettings.json` **lines 14–20** — `IdentityConnection` is `localhost/postgres/postgres`, admin password is `"ChangeThisPassword14!"` — clearly a placeholder
- `.gitignore` **lines 16–18** — `.env`, `.env.local`, `.env.*.local`
- `.gitignore` **line 8** — `appsettings.Development.json` ignored
- Railway Variables tab (show keys: `ConnectionStrings__Default`, `ConnectionStrings__IdentityConnection`, `Authentication__Google__ClientId`, `Authentication__Google__ClientSecret`)

---

### STEP 10 — Privacy Policy (GDPR)
**Points:** 1.0

**What to do on screen:**
1. Scroll to the footer of any public page — show the Privacy Policy link
2. Click it — navigate to `/privacy`
3. Slowly scroll through the entire privacy policy — all 10 sections must be on screen
4. Say: *"The privacy policy is tailored to Hope Haven and covers all GDPR-required sections: who we are, data collected, how it's used, cookies, data sharing, retention, user rights, security, changes, and contact."*

**Code to show:**
- `frontend/src/pages/public/PrivacyPage.tsx` **lines 27–130** — 10 sections: Who We Are, Information We Collect, How We Use, Cookies, Data Sharing, Retention, Your Rights, Security, Changes, Contact
- `frontend/src/components/shared/PublicLayout.tsx` **line 118** — `<Link to="/privacy">` in the footer
- `frontend/src/App.tsx` **line 75** — `/privacy` is a public route (no ProtectedRoute)

---

### STEP 11 — Cookie Consent (Functional, Accept + Decline)
**Points:** 1.0

**What to do on screen:**
1. Open an incognito/private window so localStorage is empty
2. Navigate to the site — the cookie consent banner must appear at the bottom of the screen
3. Show both the **Accept** button and the **Decline** button are present
4. Click **Decline** — show the banner disappears
5. Refresh the page — show the banner does NOT reappear (the declined preference is persisted)
6. Navigate to `/privacy` — show the **Reset Cookie Preferences** button
7. Click it — show the banner comes back on the next page load
8. This time click **Accept** — show the banner disappears again
9. Say: *"Cookie consent is fully functional — both choices persist to localStorage. Resetting from the Privacy page clears the stored preference."*

**Code to show:**
- `frontend/src/components/shared/CookieConsentBanner.tsx` **lines 23–35** — shows both Decline and Accept `<button>` elements
- `frontend/src/contexts/CookieConsentContext.tsx` **lines 21–28** — `acknowledgeConsent()` stores `'accepted'`, `declineConsent()` stores `'declined'`
- `frontend/src/contexts/CookieConsentContext.tsx` **lines 12–15** — `readInitial()` reads from `localStorage` — returns `true` for either `'accepted'` or `'declined'` so banner stays gone
- `frontend/src/pages/public/PrivacyPage.tsx` **lines 17–19** — `clearConsent()` calls `localStorage.removeItem('hh_cookie_consent')` then reloads

---

### STEP 12 — CSP Header in DevTools
**Points:** 2.0

**What to do on screen:**
1. On the live production site, open **DevTools → Network** tab
2. Click on any API request (e.g., `/api/dashboard/metrics`)
3. Click the **Response Headers** section
4. Show the `content-security-policy` header is present — read out the key directives:
   - `default-src 'self'` — only loads resources from our own domain
   - `script-src 'self'` — no external scripts
   - `img-src 'self' data:` — allows data URIs (needed for QR codes)
   - `frame-ancestors 'none'` — prevents clickjacking
   - `object-src 'none'` — blocks Flash / plugin injection
5. Also open **DevTools → Console** — show there are **no red CSP violation errors**
6. Say: *"The CSP is set server-side via middleware, not a meta tag. It restricts all resource types and prevents clickjacking and XSS vectors."*

**Code to show:**
- `backend/HopeHaven.API/Infrastructure/SecurityHeaders.cs` **lines 5–14** — full CSP string defined as a constant
- `backend/HopeHaven.API/Program.cs` **line 165** — `app.UseSecurityHeaders()` registered in the middleware pipeline

---

### STEP 13 — Site Publicly Accessible (No VPN Needed)
**Points:** 4.0

**What to do on screen:**
1. With no VPN connected, navigate to the full production URL
2. Show the site loads completely
3. Say: *"The site is deployed on Railway at a public HTTPS URL. No VPN, no corporate network, no credentials are needed to access the public-facing pages."*

**Code to show:**
- `backend/HopeHaven.API/Dockerfile` — production multi-stage Docker build
- `backend/HopeHaven.API/railway.toml` — Railway deployment config with health check at `/health`

---

### STEP 14 — Google OAuth ("Continue with Google")
**Points:** part of Additional 2.0

**What to do on screen:**
1. Navigate to `/login` — show the **"Continue with Google"** button below the login form
2. Click it — Google's OAuth consent screen appears (or directly authenticates if you're already signed in to Google)
3. Complete the flow — show that you are logged in to Hope Haven after returning from Google
4. Say: *"Google OAuth is implemented. New Google users get a Donor account created automatically. Returning users are signed in instantly."*

**Code to show:**
- `backend/HopeHaven.API/Controllers/AuthController.cs` **lines 62–78** — `ExternalLogin()` initiates the OAuth challenge redirect to Google
- `backend/HopeHaven.API/Controllers/AuthController.cs` **lines 80–115** — `ExternalLoginCallback()` handles the response: looks up existing account, creates one if new, assigns Donor role
- `backend/HopeHaven.API/Program.cs` **lines 37–48** — `AddGoogle()` registration (conditional on secrets being set)
- `frontend/src/pages/public/LoginPage.tsx` **lines 26–28** — `getExternalProviders()` fetched on mount
- `frontend/src/pages/public/LoginPage.tsx` **lines 203–212** — renders the Google button dynamically when the provider is available

---

### STEP 15 — MFA / TOTP (Authenticator App)
**Points:** part of Additional 2.0

**What to do on screen:**

**Part A — Show MFA setup:**
1. Log in with an account that has MFA enabled (use the dedicated MFA test account)
2. Navigate to the MFA management page (accessible from the user nav)
3. Show the **QR code** on screen — explain it's scanned into an authenticator app (Google Authenticator, Authy, etc.)
4. Show the **recovery codes** that are displayed for backup access
5. Say: *"Users can enable TOTP-based MFA. The QR code is generated server-side using the shared secret and rendered via the qrcode package."*

**Part B — Show MFA prompt at login:**
1. Log out
2. Log in with the MFA-enabled account credentials
3. Show the **"Have an authenticator or recovery code?"** link — click it
4. Show the **Authenticator code** and **Recovery code** input fields appear
5. Enter the 6-digit TOTP code from the authenticator app
6. Show successful login
7. Say: *"When MFA is enabled, login requires the 6-digit TOTP code from the authenticator app."*

**Code to show:**
- `frontend/src/pages/admin/ManageMFAPage.tsx` **line 3** — `import QRCode from 'qrcode'`
- `frontend/src/pages/admin/ManageMFAPage.tsx` **lines 23–29** — builds the `otpauth://totp/...` URI with issuer `"Hope Haven"` and the user's email
- `frontend/src/pages/admin/ManageMFAPage.tsx` **line 40** — `QRCode.toDataURL(uri, ...)` generates the QR code data URL
- `frontend/src/pages/admin/ManageMFAPage.tsx` **lines 43–60** — `handleEnable()` sends the verification code to enable MFA
- `frontend/src/pages/public/LoginPage.tsx` **lines 116–160** — MFA fields section: authenticator code input (line 124), recovery code input (line 139), toggle button (line 153–159)
- `frontend/src/utils/authAPI.ts` **lines 81–85** — `getTwoFactorStatus()`, `enableTwoFactor()`, `disableTwoFactor()`, `resetRecoveryCodes()` API calls

---

### Quick Pre-Recording Checklist

Before you hit record, verify all of these:
- [ ] Production site loads at Railway URL with `https://` and padlock
- [ ] DevTools → Network → any API response → `content-security-policy` header is present
- [ ] DevTools → Console → zero red CSP violation errors
- [ ] Google "Continue with Google" button shows on `/login`
- [ ] Admin account (no MFA) credentials ready
- [ ] Donor account (no MFA, has donation history) credentials ready
- [ ] MFA-enabled account credentials + authenticator app ready
- [ ] Incognito window ready for cookie consent demo
- [ ] Railway Variables tab ready to show (don't expose values, just show keys exist)

---

## Score Summary

| Requirement | Points | Estimated | Status |
|---|---|---|---|
| Confidentiality — HTTPS / TLS | 1.0 | **1.0** | ✅ |
| Confidentiality — HTTP → HTTPS redirect | 0.5 | **0.5** | ✅ |
| Auth — Username / password login | 3.0 | **3.0** | ✅ |
| Auth — NIST password policy | 1.0 | **1.0** | ✅ |
| Auth — Endpoints protected appropriately | 1.0 | **1.0** | ✅ |
| Auth — RBAC: Admin-only CUD at the API level | 1.5 | **1.5** | ✅ |
| Integrity — Confirmation before delete | 1.0 | **0.9** | ✅ |
| Credentials — Secrets not in the repo | 1.0 | **1.0** | ✅ |
| Privacy — GDPR privacy policy | 1.0 | **1.0** | ✅ |
| Privacy — Cookie consent (functional) | 1.0 | **1.0** | ✅ |
| Attack Mitigations — CSP header | 2.0 | **2.0** | ✅ |
| Availability — Publicly deployed | 4.0 | **4.0** | ✅ |
| Additional — Third-party auth (Google OAuth) | — | — | ✅ |
| Additional — MFA / TOTP | — | — | ✅ |
| **Additional (combined)** | **2.0** | **2.0** | ✅ |
| **TOTAL** | **20.0** | **20.0** | ✅ |

---

## Full Checklist

### Confidentiality

- [x] **HTTPS / TLS on all public connections**
  - Railway provisions TLS certificate automatically
  - Both backend and frontend served over `https://`
  - **Video:** Point to the padlock / `https://` in the browser address bar

- [x] **Redirect HTTP → HTTPS**
  - `backend/HopeHaven.API/Program.cs` line 160 — `app.UseHttpsRedirection()` unconditional
  - `Program.cs` line 137 — `app.UseForwardedHeaders()` runs first so proxy scheme is read correctly
  - **Video:** Mention Railway enforces proxy-level redirect + `UseHttpsRedirection()` is in code

---

### Authentication

- [x] **Username / password authentication (ASP.NET Identity)**

  | File | Lines | What it does |
  |---|---|---|
  | `Program.cs` | 31–33 | `AddIdentityApiEndpoints<ApplicationUser>()` |
  | `Program.cs` | 167 | `MapIdentityApi<ApplicationUser>()` exposes `/api/auth/register`, `/api/auth/login` |
  | `frontend/src/pages/public/LoginPage.tsx` | 1–end | Login form UI |
  | `frontend/src/pages/public/RegisterPage.tsx` | 1–end | Registration form UI |
  | `frontend/src/utils/authAPI.ts` | 52–74 | `registerUser()` and `loginUser()` |
  | `backend/HopeHaven.API/Data/AuthIdentityDbContext.cs` | 1–end | Identity DB context |
  | `backend/HopeHaven.API/Migrations/AuthIdentity/` | — | EF Core migration (Supabase) |

- [x] **Visitors can browse public pages without login**
  - Public routes in `App.tsx` lines 67–76: `/`, `/impact`, `/login`, `/register`, `/logout`, `/privacy`, `/referral` — no `ProtectedRoute`
  - `PublicImpactSnapshotsController.cs` line 17 — `[AllowAnonymous]` on GET
  - `DashboardController.cs` lines 14 and 85 — `[AllowAnonymous]` on metrics/safehouse-summary

- [x] **Authenticated users can view IS413 pages**
  - `ProtectedRoute` in `App.tsx` lines 48–57 gates admin + donor routes
  - Admin routes require `requiredRoles={['Admin']}` — `App.tsx` line 121

- [x] **NIST password policy (14+ chars, no complexity)**

  `backend/HopeHaven.API/Program.cs` lines 64–76:
  ```csharp
  options.Password.RequireDigit           = false;
  options.Password.RequireLowercase       = false;
  options.Password.RequireNonAlphanumeric = false;
  options.Password.RequireUppercase       = false;
  options.Password.RequiredLength         = 14;
  options.Password.RequiredUniqueChars    = 1;
  ```
  UI hint: `frontend/src/pages/public/RegisterPage.tsx` line 78
  - **Video:** Try registering with a short password (show it fail), succeed with 14+ chars

- [x] **All API endpoints protected appropriately**

  Every data controller has `[Authorize]` or `[Authorize(Roles = "Admin")]` at class level.

  Open endpoints (correctly public):
  - `AuthController.cs` — `/api/auth/me`, `/api/auth/providers`, `/api/auth/external-login`, `/api/auth/external-callback`
  - `ReferralsController.cs` — public referral form
  - `PublicImpactSnapshotsController.cs` line 17 — `[AllowAnonymous]`
  - `DashboardController.cs` lines 14, 85 — `[AllowAnonymous]`

  Protected controllers (require at minimum login):

  | Controller | Auth |
  |---|---|
  | `ResidentsController` | `[Authorize(Roles="Admin")]` + `[Authorize]` |
  | `DonationsController` | `[Authorize(Roles="Admin")]` + `[Authorize]` |
  | `SupportersController` | `[Authorize(Roles="Admin")]` + `[Authorize]` |
  | `EducationRecordsController` | `[Authorize(Roles="Admin")]` + `[Authorize]` |
  | `HealthWellbeingRecordsController` | `[Authorize(Roles="Admin")]` + `[Authorize]` |
  | `DashboardController` | `[Authorize(Roles="Admin")]` + `[Authorize]` (2 GETs are `[AllowAnonymous]`) |
  | `ReportsController` | `[Authorize(Roles="Admin")]` + `[Authorize]` |
  | `SocialMediaPostsController` | `[Authorize(Roles="Admin")]` + `[Authorize]` |
  | `PredictController` | `[Authorize(Roles="Admin")]` + `[Authorize]` |
  | `HomeVisitationsController` | `[Authorize(Roles="Admin")]` |
  | `IncidentReportsController` | `[Authorize(Roles="Admin")]` |
  | `InterventionPlansController` | `[Authorize(Roles="Admin")]` |
  | `ProcessRecordingsController` | `[Authorize(Roles="Admin")]` |
  | `SafehousesController` | `[Authorize(Roles="Admin")]` |
  | `PartnersController` | `[Authorize(Roles="Admin")]` |
  | `DonationAllocationsController` | `[Authorize(Roles="Admin")]` |
  | `InKindDonationItemsController` | `[Authorize(Roles="Admin")]` |
  | `UsersController` | `[Authorize(Roles="Admin")]` |
  | `MyDonationsController` | `[Authorize]` (any logged-in user) |
  | `CrudController` base | `[Authorize]` + `[Authorize(Policy="ManageContent")]` on POST/PUT/DELETE |

  - **Video:** Open DevTools → Network, hit `/api/residents` while logged out, show 401 response

---

### RBAC

- [x] **Only Admin can CUD; only Donors see their history; public sees limited pages**

  Every POST, PUT, DELETE, and PATCH has `[Authorize(Policy = AuthPolicies.ManageContent)]` which requires Admin role.

  Applied at method level in:
  - `CrudController.cs` lines 31 / 40 / 58
  - `ResidentsController.cs` lines 72, 82, 97
  - `DonationsController.cs` lines 53, 62, 77
  - `SupportersController.cs` lines 64, 74, 89
  - `EducationRecordsController.cs` lines 25, 34
  - `HealthWellbeingRecordsController.cs` lines 25, 34
  - `PublicImpactSnapshotsController.cs` lines 35, 44
  - `SocialMediaPostsController.cs` lines 44, 54, 65

  `ManageContent` policy defined: `Program.cs` lines 58–59 → `RequireRole(AuthRoles.Admin)`

  `DefaultPolicy` set in `Program.cs` lines 55–57 to use cookie auth scheme.

  - **Video:** Log in as Donor → try to edit a resident → show 403. Log in as Admin → show it works.

---

### Integrity

- [x] **Delete requires confirmation**

  `DeleteConfirmDialog` component used in:
  - `frontend/src/pages/admin/DonorManagement.tsx` lines 594–598
  - `frontend/src/pages/admin/HomeVisitations.tsx` lines 516–528
  - `frontend/src/pages/admin/ProcessRecordings.tsx` lines 558–570

  - **Video:** Click delete on a record, show confirmation dialog before deletion happens.

---

### Credentials

- [x] **Secrets stored securely, not in repo**

  | Secret | Where |
  |---|---|
  | Supabase connection string | .NET user secrets (local) / Railway env vars (production) |
  | Google OAuth Client ID + Secret | .NET user secrets / Railway env vars |
  | Admin seed password | .NET user secrets / Railway env vars |
  | `VITE_API_URL` | `frontend/.env.local` (gitignored) / Railway env vars |

  `appsettings.json` only has localhost placeholders — no real credentials.
  `.gitignore` covers `.env`, `.env.local`, `.env.*.local`.

  - **Video:** Show `.gitignore`, show `appsettings.json` has no real passwords, briefly show Railway Variables tab.

---

### Privacy

- [x] **GDPR privacy policy, tailored to site, linked from footer**
  - Page: `frontend/src/pages/public/PrivacyPage.tsx` — 10 sections
  - Footer link: `frontend/src/components/shared/PublicLayout.tsx` line 24
  - Route: `App.tsx` line 74

- [x] **GDPR cookie consent notification (functional)**
  - Banner: `frontend/src/components/shared/CookieConsentBanner.tsx` — Accept + Decline buttons
  - Context: `frontend/src/contexts/CookieConsentContext.tsx` — `localStorage` key `hh_cookie_consent`, stores `'accepted'` or `'declined'`
  - Reset button: `frontend/src/pages/public/PrivacyPage.tsx` line 18
  - **In video:** Show banner on first visit → click Decline (banner disappears, stored as declined) → revisit, confirm it stays dismissed → show Reset Cookie Preferences on Privacy page brings it back.

---

### Attack Mitigations

- [x] **CSP header set via middleware (not meta tag)**
  - Middleware: `backend/HopeHaven.API/Infrastructure/SecurityHeaders.cs` lines 5–17
  - Registered: `Program.cs` line 158 — `app.UseSecurityHeaders()`
  - Policy:
    ```
    default-src 'self';
    script-src 'self';
    style-src 'self' 'unsafe-inline';
    img-src 'self' data:;
    connect-src 'self';
    font-src 'self';
    base-uri 'self';
    frame-ancestors 'none';
    object-src 'none'
    ```
  - `style-src 'unsafe-inline'` — required for Recharts chart inline styles
  - `img-src data:` — required for QR code data URIs on MFA setup page
  - **Video:** DevTools → Network → any API response → Response Headers → show `content-security-policy` present

---

### Availability

- [x] **Site publicly accessible**
  - Backend + frontend deployed on Railway at public HTTPS URLs
  - No VPN or credentials needed to access the site

---

### Additional Security Features ✅ (2/2 pts)

#### Google OAuth (Third-Party Auth)
- [x] "Continue with Google" button on `/login`
- [x] Auto-creates account + assigns Donor role for new Google users

| File | Lines | What it does |
|---|---|---|
| `Program.cs` | 38–48 | Registers Google OAuth (conditional on secrets) |
| `AuthController.cs` | 47–63 | `ExternalLogin()` — initiates OAuth redirect |
| `AuthController.cs` | 65–101 | `ExternalLoginCallback()` — handles response, creates user |
| `frontend/src/pages/public/LoginPage.tsx` | — | Google button (shown when provider available) |

#### TOTP / Authenticator App MFA
- [x] QR code generation for authenticator app setup
- [x] Enable / disable MFA
- [x] Recovery codes displayed and resettable
- [x] MFA code entry on login page

| File | Lines | What it does |
|---|---|---|
| `frontend/src/pages/admin/ManageMFAPage.tsx` | 1–end | Full MFA management UI |
| `frontend/src/utils/authAPI.ts` | 81–85 | `getTwoFactorStatus`, `enableTwoFactor`, `disableTwoFactor`, `resetRecoveryCodes` |
| `frontend/src/pages/public/LoginPage.tsx` | — | MFA code + recovery code fields |

> **Grader accounts required:**
> - Admin account WITHOUT MFA
> - Donor account WITHOUT MFA (with donation history)
> - One account WITH MFA enabled (graders won't log in, just verify it exists)

---

## Pre-Video Security Checklist

- [ ] Deploy latest `main` to Railway so all code changes are live
- [ ] Set Railway env vars: `ConnectionStrings__IdentityConnection`, `ConnectionStrings__Default`, `Authentication__Google__ClientId`, `Authentication__Google__ClientSecret`, `FrontendUrl`, `AllowedOrigins`
- [ ] Open live site → DevTools → Console → confirm NO red CSP violation errors
- [ ] Open live site → DevTools → Network → any API response → confirm `content-security-policy` header present
- [ ] Confirm Google "Continue with Google" button shows on `/login` on deployed site
- [ ] Confirm admin account without MFA exists
- [ ] Confirm donor account without MFA (with donation history) exists
- [ ] Confirm one account with MFA enabled exists
